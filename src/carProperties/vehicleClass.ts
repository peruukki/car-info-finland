import { CarProperty } from '../types';

const property: CarProperty = {
  name: 'vehicle class',
  columnName: 'ajoneuvoluokka',
  filterValues: {
    car: ['M1', 'M1G'],
  },
};

export = property;
